<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybais.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kedu.nodazi.mapper.RecStockMapper">

	<select id="readRecList"
			resultType="list<string>">
		select code
		  from rec_stock
		 where r_regdt = #{today}
		 order by r_seq
	</select>
	
	<select id="readRecStock"
			resultType="com.kedu.nodazi.dto.RecStockDto">
		select *
		  from prices
		 where code = #{code}
	</select>

</mapper>